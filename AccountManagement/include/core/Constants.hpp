#pragma once
#include <string>
#include <filesystem>

namespace Constants {
    const std::string DATE_FORMAT = "%Y-%m-%d";
    const std::string DATA_DIR = "data";
    const std::string DATA_FILE = DATA_DIR + "/accounts.csv";
    const int MAX_NAME_LENGTH = 50;
}